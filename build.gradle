// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext.kotlin_version = '1.3.21'
    ext.appcompat_version = '1.1.0-alpha03'
    ext.core_version = '1.1.0-alpha05'
    ext.constraintlayout_version = '1.1.3'
    ext.lifecycle_version = '2.0.0'
    ext.room_version = '2.1.0-alpha05'
    ext.nav_version = '2.0.0'
    ext.junit_version = '4.12'
    ext.mockito_version = '2.25.1'
    ext.runner_version = '1.1.2-alpha02'
    ext.espresso_core_version = '3.2.0-alpha02'
    ext.okhttp_version = '3.14.0'
    ext.gson_version = '2.8.5'
    ext.retrofit_version = '2.5.0'
    ext.glide_version = '4.9.0'
    ext.dagger_version = '2.21'

    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.3.2'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath "androidx.navigation:navigation-safe-args-gradle-plugin:$nav_version"
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
